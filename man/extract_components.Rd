% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{extract_components}
\alias{extract_components}
\title{Extract the components a downstream stage needs from a simList}
\usage{
extract_components(sim, plain = character(), spatial = character(), dir = ".")
}
\arguments{
\item{sim}{A \code{simList} (or any object supporting \code{sim[["name"]]}).}

\item{plain}{Character vector of object names to return as-is.}

\item{spatial}{Character vector of spatial object names to write to files.}

\item{dir}{Directory to write spatial files into (created if needed).}
}
\value{
A named \code{list} with the \code{plain} objects plus \code{"<name>_path"} entries
for each \code{spatial} object.
}
\description{
Pulls named objects out of a (completed) \code{simList} and splits them by kind so
they can become \code{targets} of the right type, \strong{without serializing the whole
\code{simList}}:
}
\details{
\itemize{
\item \code{plain} objects (data.tables, vectors, scalars, lists) are returned as-is
and serialize cleanly via the default/\code{qs2} target format;
\item \code{spatial} objects (\code{terra}/\code{sf}) are written to disk with \code{\link[=write_spatial]{write_spatial()}}
and returned as \code{"<name>_path"} character entries, for companion
\code{format = "file"} targets.
}
}
