% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{extract_outputs}
\alias{extract_outputs}
\title{Extract a stage's output manifest from a completed simList}
\usage{
extract_outputs(sim, plain = character(), base_dir = ".")
}
\arguments{
\item{sim}{A completed \code{simList}.}

\item{plain}{Optional character vector of in-memory object names to also
return as-is. An escape hatch for small objects (vectors, data.tables,
colour tables) you would rather pass directly than round-trip through disk.}

\item{base_dir}{Directory the manifest file paths are made relative to
(default the working directory), so paths stay portable across hosts and
stable for \code{targets} file-content hashing.}
}
\value{
A named \code{list} with:
\itemize{
\item \code{manifest}: a \code{data.frame} with one row per saved file, columns
\code{objectName}, \code{file}, \code{saveTime}, \code{fun}, \code{package};
\item \code{files}: the \code{character} vector of saved file paths (the value a companion
\code{format = "file"} target should yield); and
\item any \code{plain} objects, each under its own name.
}
}
\description{
Reads \code{SpaDES.core::outputs(sim)} after a run and returns a compact manifest
of every file the simulation actually saved, \strong{without serializing the whole
\code{simList}}. Instead of declaring object names a priori, it discovers them
from the simulation's own outputs table, so runtime-determined file sets are
captured automatically. That table is fed by all of SpaDES's save mechanisms
at once:
}
\details{
\itemize{
\item objects requested via the \code{outputs} argument to \code{SpaDES.core::simInit()}
(e.g. per-timestep saves of \code{vegTypeMap}, \code{standAgeMap}, ...);
\item files registered by a module with \code{SpaDES.core::registerOutputs()} (e.g. a
summary module dumping a timeseries of objects in "single" mode); and
\item figures written by \code{SpaDES.core::Plots()} (the saved \code{.png}/\code{.pdf} files are
appended to \code{outputs(sim)}).
}
}
\seealso{
\code{\link[=sim_inputs]{sim_inputs()}} turns a manifest into a downstream \code{simInit(inputs=)}
table; \code{\link[=tar_simspades]{tar_simspades()}} wires both into a pipeline.
}
