% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outputs.R
\name{outputs_spec}
\alias{outputs_spec}
\title{Build a \code{simInit(outputs=)} table for terra and RDS objects}
\usage{
outputs_spec(
  raster = character(),
  vect = character(),
  rds = character(),
  qs = character(),
  csv = character(),
  saveTime = NULL
)
}
\arguments{
\item{raster}{Character vector of object names to save with
\code{terra::writeRaster()} (\code{.tif}).}

\item{vect}{Character vector of object names to save with
\code{terra::writeVector()} (\code{.gpkg}).}

\item{rds}{Character vector of object names to save with \code{saveRDS()}
(\code{.rds}).}

\item{qs}{Character vector of object names to save with \code{qs2::qs_save()}
(\code{.qs}); read back with \code{qs2::qs_read()} (see \code{\link[=sim_inputs]{sim_inputs()}}).}

\item{csv}{Character vector of object names to save with
\code{data.table::fwrite()} (\code{.csv}) -- for flat tables consumed downstream (e.g.
LANDIS-II inputs). \code{fwrite} writes no row names, unlike \code{utils::write.csv()}.}

\item{saveTime}{Optional numeric vector of save times; the rows are expanded
over every (object, time) combination. \code{NULL} (default) lets
\code{SpaDES.core} save once at \code{end(sim)}.}
}
\value{
A \code{data.frame} with \code{objectName}, \code{fun}, \code{package} (and \code{saveTime}
when supplied), suitable as \code{SpaDES.core::simInit(outputs=)}. Filenames are
left for \code{SpaDES.core} to derive (objectName + time + extension).
}
\description{
Convenience constructor for the \code{outputs} \code{data.frame} that
\code{SpaDES.core::simInit()} consumes, declaring which simulation objects to save
to disk and how. Objects are grouped by how they should be written --
\code{terra::writeRaster()}, \code{terra::writeVector()}, or \code{saveRDS()} -- and the
table is expanded over \code{saveTime} so an object can be saved at several points
in simulation time. The saved files then appear in \code{outputs(sim)} and are
picked up by \code{\link[=extract_outputs]{extract_outputs()}}.
}
\details{
This covers the common inter-stage handoff and timeseries cases; for anything
more exotic (per-object save \code{arguments}, mixed save functions for one
object) build the \code{data.frame} directly -- see \code{?SpaDES.core::outputs}.
}
\examples{
outputs_spec(
  raster = c("rasterToMatch", "rstLCC"),
  vect = c("studyArea", "studyAreaReporting")
)
}
\seealso{
\code{\link[=extract_outputs]{extract_outputs()}}, \code{\link[=sim_inputs]{sim_inputs()}}
}
