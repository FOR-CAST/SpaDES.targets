% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inputs.R
\name{sim_inputs}
\alias{sim_inputs}
\title{Build a \code{simInit(inputs=)} table from an upstream manifest}
\usage{
sim_inputs(manifest, objects = NULL, at = NULL, loadTime = NULL, files = NULL)
}
\arguments{
\item{manifest}{A manifest \code{data.frame} (the \code{manifest} element of an
\code{\link[=extract_outputs]{extract_outputs()}} result) or that whole result \code{list}.}

\item{objects}{Optional character vector restricting which \code{objectName}s to
load; defaults to every object in \code{manifest}.}

\item{at}{Optional numeric \code{saveTime} to select; defaults to the most recent
save of each object.}

\item{loadTime}{Optional numeric \code{loadTime} passed through to the \code{inputs}
table (when the file should be loaded in simulation time).}

\item{files}{Optional character vector of tracked output file paths (the
value of the companion \verb{<stage>_files} target). When supplied, every
selected file must be present in it.}
}
\value{
A \code{data.frame} with \code{file} and \code{objectName} columns (plus \code{loadTime}
when supplied), suitable as \code{SpaDES.core::simInit(inputs=)}.
}
\description{
Turns the \code{manifest} produced by \code{\link[=extract_outputs]{extract_outputs()}} into the \code{inputs}
\code{data.frame} that \code{SpaDES.core::simInit()} consumes, so a downstream stage
loads its dependencies from the files the upstream stage wrote -- symmetric
with how they were saved, and without loading them into the orchestrating
process. The load function is left to \code{SpaDES.core} to deduce from each
file's extension.
}
\details{
Pass the companion \code{format = "file"} target (the \verb{<stage>_files} target from
\code{\link[=tar_simspades]{tar_simspades()}}) as \code{files} so that the downstream target gains a
file-content dependency on the upstream outputs (otherwise \code{targets} would
only see the manifest paths, not their contents) and so that the requested
files are validated against what was actually tracked.
}
\seealso{
\code{\link[=extract_outputs]{extract_outputs()}}, \code{\link[=tar_simspades]{tar_simspades()}}
}
