% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inputs.R
\name{sim_objects}
\alias{sim_objects}
\title{Load an upstream manifest's outputs into memory for \code{simInit(objects=)}}
\usage{
sim_objects(manifest, objects = NULL, at = NULL, files = NULL)
}
\arguments{
\item{manifest}{A manifest \code{data.frame} (the \code{manifest} element of an
\code{\link[=extract_outputs]{extract_outputs()}} result) or that whole result \code{list}.}

\item{objects}{Optional character vector restricting which \code{objectName}s to
load; defaults to every object in \code{manifest}.}

\item{at}{Optional numeric \code{saveTime} to select; defaults to the most recent
save of each object.}

\item{files}{Optional character vector of tracked output file paths (the
value of the companion \verb{<stage>_files} target). When supplied, every
selected file must be present in it.}
}
\value{
A named \code{list} mapping \code{objectName} to the loaded object, suitable to
splice into \code{SpaDES.core::simInit(objects=)}.
}
\description{
The counterpart to \code{\link[=sim_inputs]{sim_inputs()}}: instead of building a \code{simInit(inputs=)}
table for \code{SpaDES.core} to load at run time, this loads the requested objects
from disk \strong{now} (on the worker) and returns them as a named list, so they are
available during \code{simInit()} itself -- in particular to a module's
\code{.inputObjects()}, which runs before \verb{inputs=} are loaded. Use this for spatial
handoff objects a downstream module touches in \code{.inputObjects()} (e.g.
\code{Biomass_borealDataPrep} reads \code{sim$studyArea} / \code{sim$rasterToMatch} there); use
\code{\link[=sim_inputs]{sim_inputs()}} for objects a module needs only once events run.
}
\details{
Objects are loaded with the reader matching each manifest row's save function
(\code{terra::rast} / \code{terra::vect} / \code{base::readRDS} / \code{qs2::qs_read} /
\code{data.table::fread}). \code{terra} rasters/vectors load lazily, so this is cheap even
for large layers.
}
\seealso{
\code{\link[=sim_inputs]{sim_inputs()}}, \code{\link[=extract_outputs]{extract_outputs()}}
}
