% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{write_spatial}
\alias{write_spatial}
\title{Write a spatial object to a file (for a \code{format = "file"} target)}
\usage{
write_spatial(x, path, overwrite = TRUE)
}
\arguments{
\item{x}{A \code{SpatRaster}, \code{SpatVector}, or \code{sf}/\code{sfc} object.}

\item{path}{Destination file path.}

\item{overwrite}{Overwrite an existing file? Defaults to \code{TRUE}.}
}
\value{
\code{path}, invisibly.
}
\description{
Writes a \code{terra} \code{SpatRaster} (default extension \code{.tif}) or \code{SpatVector}
(default \code{.gpkg}) and returns \code{path}. \code{sf}/\code{sfc} inputs are coerced to
\code{SpatVector} first (the project prefers \code{terra} over \code{sf}, and \code{geotargets}
has no \code{sf} path). The returned path is what a \code{targets} \code{format = "file"}
target should yield so that \code{targets} hashes the file's contents.
}
\seealso{
\code{\link[=read_spatial]{read_spatial()}}
}
