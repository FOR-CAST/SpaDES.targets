# Is an object a spatial object?

Is an object a spatial object?

## Usage

``` r
is_spatial(x)
```

## Arguments

- x:

  An object.

## Value

`TRUE` if `x` is a `terra`, `sf`, `raster`, or `sp` spatial object.
