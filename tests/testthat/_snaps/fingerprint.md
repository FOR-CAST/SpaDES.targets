# tar_simspades() honours the fingerprint option and a supplied vector

    Code
      tar_simspades("s", modules = "m", fingerprint = 1)
    Condition
      Error:
      ! `fingerprint` must be TRUE, FALSE, NULL, or a character vector.

